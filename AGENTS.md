# Florida Signal site repository instructions

These instructions apply to this repository and all paths below it.

## Required startup

Before acting in a new Codex chat, read these current-state authorities in order:

1. `/Users/gillfillan/Documents/FL SIGNAL SITE BUILD/.worktrees/florida-signal-state-reconciliation/AGENTS.md`.
2. `/Users/gillfillan/Documents/FL SIGNAL SITE BUILD/.worktrees/florida-signal-state-reconciliation/docs/FLORIDA_SIGNAL_START_HERE.md`.
3. `/Users/gillfillan/Documents/FL SIGNAL SITE BUILD/.worktrees/florida-signal-state-reconciliation/data/reference/florida_signal_project_state.json`.
4. `/Users/gillfillan/Documents/FL SIGNAL SITE BUILD/.worktrees/florida-signal-state-reconciliation/docs/SESSION_HANDOFF_2026-08-30.md`.
5. This worktree's `SYSTEM_STATE_2026-08-30.md` only as a dated implementation checkpoint; the four authorities above win on any conflict.

Treat the manifest as durable project state, not live health. Verify current operational claims from their declared live receipts; missing health remains `UNKNOWN`.

When work materially changes source coverage, automation, production state, release state, safety
gates, or the current priority, update this checkpoint and the canonical state authorities in the
same workstream before reporting completion. If an authority cannot be updated safely, report the
drift explicitly instead of leaving a silent contradiction.

At task start, state the repository top level, branch, status, HEAD, and `origin`. Run the equivalent of `git rev-parse --show-toplevel`, `git status --short --branch`, `git branch --show-current`, `git log -1 --oneline`, and `git remote get-url origin` before editing.

## Worktree guard

Also inspect `/Users/gillfillan/Documents/FL SIGNAL SITE BUILD` at task start, every 30 minutes during long work, and immediately before any commit, push, merge, deploy, or completion report. Recheck its repository top level, branch, status, HEAD, and `origin`; do the same for the active worktree before a Git or release action.

Assume dirty or untracked files are user work. Preserve them. Do not clean, reset, checkout, restore, stash, overwrite, reformat, stage, or commit unrelated changes. Stop and report any overlap that cannot be safely isolated.

## Current truth

- Fort Lauderdale Preliminary Development Meeting Request (PDMR) records are public. The 27-record research cohort is **frozen**, meaning its roster and adjudication are fixed; the records are not access-locked, and their first-public timestamps remain unresolved.
- PDMR is the first built private Early Radar lane, not the whole product. Sewer/utility and engineering have pushed file-only shadow head `0febf30`. Its V5 production-data canary passed with 137,942 rows scanned, 523 admitted, zero rejected and 137,419 unknown; the earlier V4 sidecar-change failure remains preserved. It remains unscheduled, unpersisted, unscored and not Desk-connected. Assemblage plus new LLC and lobbyist registrations remain planned; SFWMD remains shadow-observed/not connected.
- Production Supabase has no PDMR table, function, cron, mirror, or queue. The isolated 329-record stage is reconciled at `bb91cd9`: 30 current receipts were copied, 299 exact official details were re-observed, and identity/version parity is 329/329 with zero issues. Safe schema/admission/mirror writer coordination is pushed at `2cecec1`; the disabled systemd templates remain unchanged pending a separate service-lock design approval. The sealed stage, manifest and transfer envelope are installed in the private runtime, but the production schema preflight is still pending and no production PDMR object or row has been created. Do not guess or silently repair folios.
- Permit normalization hardening is pushed on `codex/accela-health-truth-2026-08-30` at `476475614adf8d8850b691209c06f261146dc0d9` and was surgically deployed without replacing the dirty runtime checkout. A protected 11,945,189,376-byte SQLite backup passed `quick_check`; the approved dry-run bound an exact 25-row cohort, and the atomic execute updated and read back exactly those 25 rows successfully. The raw-owner normalization gap moved from 97,653 to 97,628. The normal sync completed successfully at 23:15:23 ET and exact Supabase readback proved 25 cloud rows, 25 normalized matches, 25 canary-clock matches and zero mismatches; a global clock query found exactly those 25 stamps and zero unexpected rows. Rollback evidence is ready but unused. A post-canary audit then found 27 new permits, including 12 with raw owners but zero normalized owners, and the gap rose to 97,640 because the live Accela detail scraper omitted deterministic normalization when later filling a missing owner. At about 23:55 ET the exact pushed scraper SHA-256 `ad56cc0e3435a9dcf6d45cfcca10ab9a147412420a62814e970c0fc85409b612` was atomically installed after coverage by the combined 45-test focused suite and compile checks. The patch adds one required import plus two functional same-transaction owner-mirror/parent-propagation write sites; it changed no existing row, ran no backfill, and left the service idle with no restart or timer/cadence change. The earlier 0-useful natural run did not exercise that path; later 15/150 and 13/150 useful-work runs occurred, but no row-window audit established whether owner normalization was exercised, so the current exercise state is `UNKNOWN`. No full backfill, timer/cadence change, manual mirror, collector restart or public-search diagnostic occurred.
- The production AI permit cleaner's canonical owner/contractor safety file was atomically updated at about 23:58 ET from live SHA-256 `999c6912f1edf5bfa5b6738af0447ab060de800116eb49dafcfd37bc4e0bffbc` (an exact match for git base `85341f1`) to reviewed/pushed SHA-256 `352a5ae37a85a08241d19b46b5c2d1becbdb5c1f4a6c04f02d4ee276892cf293`. Its SQL write set excludes `owner_normalized` and `contractor_normalized`, so Claude's proposed values for those fields remain only in `ai_clean_json`; exact response-ID validation protects row association, and the atomic batch protects transaction integrity. Its allowed classifications remain non-authoritative enrichment. The verified live file is 26,699 bytes, mode `0664`, owner/group `andy:andy`, with mtime 2026-08-30 23:58:18 ET. This script runs as `ai_clean_permits.py --skip-budget` in `fs_pipeline.sh intake`, owned by `florida-intake.service` and its unchanged daily 22:00 timer—not by the enrichment lane. The intake service last ran 22:00:02–22:28:19 ET with result success/exit 0 and remained inactive/dead before, through and after the install. Live wrapper SHA-256 is `4b47f72bb1518e50231f5fc355d6a847d3557d00bb75f83f24d0a4913dc3233f`. No post-install natural intake run has exercised this file yet; the next unchanged daily intake is 22:00 ET. There was no row mutation, backfill, restart or timer/cadence change at install.
- The last pre-safety intake proves the old AI identity write path was exercised, not merely reachable. At 22:01 it cleaned 27/27 new permits in three batches with zero errors. A privacy-preserving cohort audit found 12 valid raw owners with zero normalized owners and 12 differences from the current deterministic owner rule; 13 raw contractors had 13 normalized values, with one differing from the current deterministic contractor rule. All 27 current owner/contractor values match the stored AI proposals. A whole-corpus comparison using the deployed deterministic function found 97,502 valid-owner keys missing, 832 present owner keys differing, 130 valid-contractor keys missing and 6,262 present contractor keys differing; these are audit candidates, not automatically corrupt rows. No repair or expanded backfill was authorized or run.
- Accela useful-work recovery is pushed at `d646551` and its exact two runtime files were surgically installed at 2026-08-31 18:22:49 EDT with rollback copies: scraper SHA-256 `fa18dfe3ba340f50035d2cd450ec07c743218ae61591fd0f6c0cd7c3fdb9db1d` and wrapper SHA-256 `44a08302c4fe781f54e665a5b8908ddfc47ce8e018923cd823967fd8647378a7`. Lock and timer/cadence are unchanged. The first natural installed-code run completed at 18:40:38 with exact terminal accounting `150/150/15/135/0` (selected/attempted/ok/not-found/failed), elapsed 633 seconds, and locks free. The later 19:00 run closed `150/150/13/135/2` with rc0 and normal sync succeeded at 19:09:37. This resolves the zero-useful-work condition for those runs; it does not by itself prove complete fresh discovery. Evidence commit `fa6bc83` is pushed.
- Acclaim collector-health code is pushed at `2f77630f60e42b64cbf8629bb0bd93bd8dd2bb44`; Supabase migration `20260831005904` was applied and the collector runtime installed with the existing LaunchAgent plist and cadence unchanged. The required two-of-two natural observation gate remains closed. The latest independently checked run is `f28fe3d7-37f7-4697-b9e5-551296954b67` at 19:00:05–19:00:09 ET: truthful `source_wait`, reason `source_not_authoritative_yet; terms_acceptance_required`, rows 0/0, attempted through 2026-08-31, verified through 2026-08-26 and event through 2026-08-28. Its mode-`0600` local receipt SHA-256 is `c08563d34b114959f74cc1876d4c4805a615ecd56f7295518d9f7dfd22ac707d`, and exact selected-field parity was verified against Supabase row 16; the Supabase table has no receipt-SHA column, so no remote SHA parity is claimed. No plist/cadence change occurred.
- The current Supabase parcel mirror is exactly 532,470 rows and is incomplete against the official 554,358-row source. Live migration version `20260831220548` installed only the private, RLS-forced, default-off receipt/generation/range/stage foundation; all four new tables remain empty and the live parcel set was unchanged. Atomic FDEP/FAA commit code is pushed at `09503e6`, but its separate migration, secret, deployments and bounded canaries remain approval-gated; durable FDEP/FAA receipt count is still zero.
- Every pull must deterministically parse, type-normalize, validate and deduplicate new or changed raw rows and preserve raw evidence plus a versioned run receipt, including empty/unchanged polls. Do not repeatedly re-clean unchanged rows; reprocess only when parser/normalizer versions change. Claude/Grok are optional post-admission audit or enrichment, never collectors, required cleaners or health authorities.
- The current priority is existing-source data integrity and admission proof: preserve the successful Accela natural receipts and no-write historical-identity preview at pushed head `c6f106b`, then run only the separately approved maximum-three deterministic correction canary; do not infer authority for a full backfill. Next, finish the separately gated FDEP/FAA atomic receipts and parcel repair, then PDMR schema/admission gates. The PDMR read-only full preflight was safely deferred after its five-minute cutoff produced no hash; both target tables remain absent. Grok remains advisory.
- The local Finder Desk was rebuilt, opened and privately browser-verified at pushed UI head `84829b2`. It separates event/fetch/terminal-receipt clocks, keeps FDEP/FAA row availability distinct from health, orders PDMR before permits, and fixes the desktop Project-state readability failure. Validation is 96 JavaScript + 62 Python unit tests, 34 focused tests, 25 public browser passes with six intentional private skips, and 2/2 current-app private layout/Data Explorer checks. This remains a local operator surface, not a public site/API deployment. Never describe pushed, built, automated, deployed, validated, connected, healthy or live as interchangeable states.
- Keep the private PDMR-first Newsroom sequence distinct from the public map-first Data Room.

## Mutation and approval boundary

Begin read-only and keep changes within the user's stated scope. Commit or push only when the current task explicitly authorizes it. Merge, deploy, production service restart, timer/cron changes, Edge deployment or secret rotation, database migrations/writes/grant or RLS changes, wet mirrors, queue writes, publication/newsletter sends, and external communications such as the City records request all require explicit owner approval for that specific action. Never infer production authority from permission to edit, test, commit, or push code.
