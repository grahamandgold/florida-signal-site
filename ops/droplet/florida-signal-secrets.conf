# The public API and pipeline share only the parent directory, never credentials.
# andy needs traverse permission to read the pipeline-owned .env reached through
# /srv/grahamandgold/florida-signal/app/.env. public-site.env stays root:root 0600.
d /srv/grahamandgold/florida-signal/secrets 0710 root andy -
