# Florida Signal — logo & avatar exports

Generated from your original emblem artwork (791 × 783, clean edges). Emblem geometry untouched — no redraw, no arrow.

## Source files
`assets/mark-full-color.png` — navy state, blue + teal towers. Primary.
`assets/mark-white.png` — white knockout. For navy, electric blue and photography.
`assets/mark-navy-mono.png` — single-colour navy. Print, stamps, fax-grade reproduction.

`assets/symbol.png` and `assets/symbol-rev.png` are kept as aliases (full colour / white) so the landing page and the earlier logo handoff pick up the real artwork with no code change.

## exports/logos — 6 files, captured at 2×
| File | Use |
|---|---|
| `fs-lockup-horizontal-transparent.png` | Primary lockup, any light background |
| `fs-lockup-horizontal-navy.png` | Primary lockup, Atlantic navy |
| `fs-lockup-compact-transparent.png` | No tagline — footers, small sizes |
| `fs-lockup-compact-navy.png` | No tagline, navy |
| `fs-lockup-stacked-transparent.png` | Square-ish placements |
| `fs-lockup-stacked-navy.png` | Square-ish placements, navy |

Transparent versions carry no background — they sit on white, cream or imagery.

## exports/avatars — 13 files
Navy square is the primary avatar: `1024 · 512 · 400 · 256 · 180 · 32`.
Navy circle (`1024 · 400`) for platforms that mask to a circle — inset slightly further so masking never clips the peninsula.
White square (`1024 · 400`), electric blue square (`1024 · 400`), cream square (`1024`).

Platform mapping: LinkedIn and X → 400. Substack → 256. Apple touch icon → 180. Favicon → 32. YouTube and anything print → 1024.

## exports/symbol — 7 transparent PNGs
Full colour at 1024 / 512 / 256; white reverse at 1024 / 512; navy mono at 1024 / 512.

## Naming
`fs-[type]-[colourway]-[size].png` — sortable and unambiguous in a shared drive.

## One remaining upgrade
These are high-resolution raster. For billboard, vehicle wrap or large-format print, an SVG or AI original would still be worth having — everything here is sharp to roughly 1024px and no further. Nothing on the site or in social needs more.
